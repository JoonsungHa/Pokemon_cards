/**
 * Placeholder: connect wallet (e.g. MetaMask, WalletConnect).
 * Wire up useContractInteraction or wallet SDK when ready.
 */
export function ConnectButton() {
  return (
    <button type="button" className="btn btn--wallet" onClick={() => console.log('Connect wallet')}>
      Connect Wallet
    </button>
  )
}
