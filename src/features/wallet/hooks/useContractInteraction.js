/**
 * Placeholder: blockchain contract reads/writes (e.g. NFT mint, transfer).
 * Wire up ethers/viem + Alchemy/Infura when ready.
 */
export function useContractInteraction() {
  return {
    // connect, mint, transfer, etc.
  }
}
